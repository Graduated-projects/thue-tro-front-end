version: 0.2

phases:
  pre_build: 
    commands:
      - npm install
  build:
    commands:
      - node -v
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build