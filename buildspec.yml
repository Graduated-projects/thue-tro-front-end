version: 0.2

phases:
  pre_build: 
    commands:
      - npm install
  build:
    commands:
      - n latest
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: build